{% extends 'SatoripopCropImagesBundle:Layout:layout.html.twig' %}

{% block body -%}
    <div class="page-header">
        <h1>{{ 'title.new'|trans({},'SatoripopCropImagesBundle') }}</h1>
    </div>
    {% form_theme form 'SatoripopCropImagesBundle:Form:forms.html.twig' %}
    {{ form(form) }}
    <div class="btn-group btn-group-xs">
        <a class="btn btn-primary" href="{{ path('image') }}">
            <span class="glyphicon glyphicon-chevron-left"
                  title="{{ 'action.back'|trans({},'SatoripopCropImagesBundle') }}"></span> {{ 'title.back'|trans({},'SatoripopCropImagesBundle') }}
        </a>
    </div>
{% endblock %}
