{% extends 'SatoripopImagesBundle:Layout:layout.html.twig' %}

{% block body -%}
    {% form_theme delete_form 'SatoripopImagesBundle:Form:icon_button.html.twig' %}

    <h1>{{ 'title.edit'|trans({},'SatoripopImagesBundle') }} <span class="badge pull-right">{{ form(delete_form) }}</span></h1>
    {% form_theme edit_form 'SatoripopImagesBundle:Form:forms.html.twig' %}
    {{ form(edit_form) }}
    <div class="btn-group">
        <a class="btn btn-primary btn-xs" href="{{ path('image') }}"><span class="glyphicon glyphicon-chevron-left" title="{{ 'action.back'|trans({},'SatoripopImagesBundle') }}"></span> {{ 'title.back'|trans({},'SatoripopImagesBundle') }}</a>
    </div>
{% endblock %}
